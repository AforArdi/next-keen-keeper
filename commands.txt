    In my TimelineCard.jsx I did friend.friend.name because I did this: {friend: clickedFriend, action: btnAction} inside DetailsPageButtons.jsx

Now inside my TimelinePage it's showing my the key prop error here:
<TimelineCard
                                key={friend.id}
                                friend={friend}
                                formattedDate={formattedDate}
                                btnAction={friend.action}
                                Icon={icons[friend.action]}
                                ></TimelineCard>

I can't access the key now, is there any other way to write this - {friend: clickedFriend, action: btnAction}
so that this key prop problem doesn't showup
